<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nasa Picture of The Day</title>
    <link rel="stylesheet" href="styles/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Poppins&family=Whisper&display=swap" rel="stylesheet">
</head>
<body>
    <% if (locals.data) { %>
        <div class="container">
            <h1><%= data['title'] %></h1>
            <img src="<%= data["url"] %>" alt="">
            <p><%= data['explanation'] %></p>
            <p>DATE <%= data['date'] %></p>
        </div>
    <% }else{ %>
      <div class="form-container">
        <h1>Nasa Picture of the Day</h1>
        <form method="get" action="/picture">
            <button typ>GET A PICTURE OF THE DAY</button>
        </form>
    </div>
    <% } %>
</body>
</html>